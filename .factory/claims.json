[
  {
    "id": "hard-session-cap",
    "claim": "Never schedules beyond your session cap",
    "where": "Landing page and forecast controls",
    "test": "npm test -- -t @claim:hard-session-cap",
    "sandbox": "The shipped 320-overdue sample is simulated under every recovery policy; each visible day is at or below the configured 30-minute cap."
  },
  {
    "id": "csv-import",
    "claim": "Imports a queue summary CSV",
    "where": "Import controls and README",
    "test": "npm run test:e2e -- --grep @claim:csv-import",
    "sandbox": "A fresh browser imports the documented summary CSV and observes every imported queue value."
  },
  {
    "id": "offline-reload",
    "claim": "Works offline after the first visit",
    "where": "README and offline page",
    "test": "npm run test:e2e -- --grep @claim:offline-reload",
    "sandbox": "A fresh browser opens ?demo=1, waits for service-worker control, goes offline, reloads, and observes the forecast."
  },
  {
    "id": "local-only",
    "claim": "Sample planning data stays on this device",
    "where": "Landing page, demo banner, and privacy page",
    "test": "npm run test:e2e -- --grep @claim:local-only",
    "sandbox": "A fresh demo browser records every request while it selects and saves a sample plan; all observed origins are the app origin."
  }
]
